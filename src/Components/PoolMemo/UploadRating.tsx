import { memo } from "react"

const UploadRating = memo(() =>{
  return (
    <div
        className={` flex flex-col items-center justify-center  w-[100vw] h-[100vh] px-[20px] bg-[#10141E] bg-opacity-90 absolute z-50 transform transition-all duration-30`}
      >
      
    </div>
  )
})

export default UploadRating
